const num1 = 5
const num2 = 10

function addValues() {
    console.log(`the sum is :${num1 + num2}`)
}

addValues()//once required will only run and execute and no return value since no export is done.
